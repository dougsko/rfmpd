// Package daemon orchestrates the RFMP node: receiving and transmitting frames,
// managing the transmission queue, broadcasting SVEC sync vectors, and bridging
// between the RF network and the HTTP/WebSocket API.
package daemon
