// Package protocol implements the RFMP v0.4 wire format using Protocol Buffers.
// It provides frame encoding/decoding, message ID generation, fragmentation,
// and timestamp conversion between epoch (wire) and ISO (API/storage) formats.
package protocol
